pipeline {
  agent any
  stages {
    stage('Hello') {
      steps {
        sh 'terraform init'
        sh 'terraform -v'
      }
    }
    stage ("terraform Action") {
      steps {
        echo "Terraform action is --> ${action}"
        sh ('terraform ${action} --auto-approve') 
        }
      }
    }
}
